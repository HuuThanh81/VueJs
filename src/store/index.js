import { createStore } from 'vuex'
import AuthStore from '../store/modules/auth'
export default createStore({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    AUTH: AuthStore
  }
})
